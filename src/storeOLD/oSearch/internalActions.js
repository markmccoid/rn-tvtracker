import _ from "lodash";

// -- INTERNAL ACTIONS --------------------------------
// -- These are actions that are used only by other actions.
// -- Meaning they are not called by external processes.
export const tagResults = ({ state, effects }) => {
  console.log("internal FUNCTINO");
};
